# IAM role for Datafy controller on EKS

This example shows all supported input modes for the Datafy controller IRSA module:

- `oidc_provider_arn` only
- `cluster_name` only
- `cluster_name` and `oidc_provider_arn` together

It also includes an inline permissions example.
